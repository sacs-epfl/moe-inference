#!/bin/bash

# cd ${WORKSPACE}/deepspeed
# pip install --user -e .
# cd ${WORKSPACE}
# cd ${WORKSPACE}/benchmarks/promptsource
# pip install --user -e .

cd ${WORKSPACE}
cp .tmux.conf  ~/.tmux.conf
sleep infinity
